apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: LoadBalancer
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80 # The port the world sees(the shop  in the  mall)
      targetPort: 5000 # The port your container runs internally(for the cook)
